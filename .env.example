# MySQL 数据库配置
DB_HOST=localhost
DB_USER=root
DB_PASS=123456
DB_NAME=program1

# 后端服务端口
PORT=3000

# JWT 配置
JWT_SECRET=请替换为一个足够复杂的随机字符串
JWT_EXPIRES_IN=7d

# 智谱 GLM 配置（AI 功能）
# 获取方式：https://open.bigmodel.cn/ 控制台 -> API Keys
GLM_API_KEY=请填写你的智谱APIKey
# 模型名称，免费额度可用 glm-4-flash，更强可换 glm-4-plus / glm-4
GLM_MODEL=glm-4-flash
# 单次请求超时（毫秒），生成题目等长文本建议放宽
GLM_TIMEOUT=60000
